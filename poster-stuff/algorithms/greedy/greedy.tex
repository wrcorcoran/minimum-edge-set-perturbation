% how to convert to pdf:
% brew install imagemagick
% convert -density 300 {name}.pdf -transparent white {name}.png

\documentclass{article}
\usepackage{algpseudocode}
\usepackage{xcolor}
\usepackage{pagecolor}

% Set text color to 163e64
\definecolor{customcolor}{HTML}{ffffff}
\definecolor{bgcolor}{HTML}{163e64}

% Set background to transparent (white in PDF viewers)
\pagecolor{gray}
\pagecolor{bgcolor}

\begin{document}
\color{customcolor}

\noindent\textbf{GeneralGreedyPtb}
\begin{algorithmic}
% get subgraphs
\State $valEdges \gets []$
\State $ptbRate \gets $ value $\geq 0$
\State $budget \gets ptbRate \times |G.edges|$
\\
\For {$u \in G.nodes$}
\For {$v \in G.nodes$}
\If {$u \neq v$ and $\texttt{trueHeuristic}(u, v)$}
    \State $append(\{u, v\}, valEdges)$
\EndIf
\EndFor
\EndFor
\\
\While {under budget}
    \State $edge \gets \texttt{randSel}(valEdges)$
    \If {$edge \notin G}$
        \State $addEdge(G, edge)$
        \If {accuracy changes}
            \State $removeEdge(G, edge)$
        \EndIf
    \EndIf
\EndWhile

\end{algorithmic}

\end{document}
